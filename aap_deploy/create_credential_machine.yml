---
- name: Create a project
  hosts: localhost
  vars_files:
    - myvars
  connection: local
  gather_facts: false
  tasks:
    - name: Create Machine Credential
      ansible.controller.credential:
        name: "{{ credential_machine_name }}"
        organization: Default
        credential_type: Machine
        inputs:
          username: "{{ credential_machine_user }}"
          password: "{{ credential_machine_password }}"
          become_method: sudo
          become_password: "{{ credential_machine_password }}"
        controller_host: "{{ controller_host }}"
        controller_username: admin
        controller_password: "{{ _controller_password_ }}"
