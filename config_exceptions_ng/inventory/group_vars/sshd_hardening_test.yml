---
# Component test in progress: sshd hardening.
# A group value replaces the whole dict (no merge), so the baseline keys from
# all.yml are repeated here on purpose.
sshd_settings:
  MaxAuthTries: 3
  ClientAliveInterval: 300
  PasswordAuthentication: "no"
