all:
  children:
    # --- Platform ---
    rhel9:
      hosts:
        server1:
        server2:
        server3:
        server4:
    rhel10:
      hosts:
        server5:
        server6:
        server7:
        server8:

    # --- Function ---
    db_servers:
      hosts:
        server1:
        server5:
    webservers:
      hosts:
        server3:
        server7:
    appservers:
      hosts:
        server4:
        server8:
    bastion_hosts:
      hosts:
        server2:
    sap:
      hosts:
        server6:

    # --- Lifecycle environments ---
    dev:
      hosts:
        server8:
    test:
      hosts:
        server4:
    prod:
      hosts:
        server1:
        server2:
        server3:
        server5:
        server6:
        server7:

    # --- Network zones ---
    dmz:
      hosts:
        server2:
        server3:
        server7:

    # --- Compliance scopes ---
    pci_scope:
      hosts:
        server6:
        server7:

    # --- Component tests (temporary, one host per stage) ---
    sshd_upgrade_test:        # promoted to all.yml — empty until the next upgrade
      hosts: {}
    sshd_hardening_test:
      hosts:
        server8:
        server4:
        server3:
